import styles from "./sideNavbar.module.css"

export const SideNav = () => {
  return (
    <div className={styles.navContainer}>
        nav container
      <div>
        nav wrapper for padding
        <div>
          site icon & name container
        </div>
        <div>
          nav buttons container
        </div>
      </div>
    </div>
  )
}
